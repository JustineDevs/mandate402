# mandate402-backend-runtime tests

## Should trigger

1. "Add validation and health reporting to the mandate attempt route."
2. "Make Postgres runtime use a pooler-safe URL and direct schema URL."
3. "Refactor a route to preserve request-path vs worker-path boundaries."

## Should not trigger alone

1. "Design the landing page hero"
2. "Update contract ABI ownership"
3. "Implement worker retries"

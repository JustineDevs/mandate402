interface:
  display_name: "Mandate402 Responsive QA"
  short_description: "checking or changing Mandate402 responsive layouts, mobile behavior, desktop density, landing split layout, modals, tables, drawers, or visual QA"
  default_prompt: "Use $mandate402-responsive-qa for this task and follow its SKILL.md, references, and TESTS.md before implementing."
policy:
  allow_implicit_invocation: true

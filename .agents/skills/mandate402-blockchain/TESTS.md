# mandate402-blockchain tests

## Should trigger

1. "Normalize Morph network configuration and contract registry for the app runtime."
2. "Move inline ABI ownership into curated ABI files and update the chain client usage."
3. "Expose treasury and chain readiness in /api/system."

## Should not trigger alone

1. "Restyle the policies page"
2. "Refactor the worker queue retry loop"
3. "Tune dashboard card spacing"

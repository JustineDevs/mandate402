interface:
  display_name: "Mandate402 Backend"
  short_description: "the backend task is broad or spans multiple runtime lanes in Mandate402 and you need a strict router for backend-runtime, blockchain, and workers plus the shared rules for validation, security, DB behavior, and deployment-grade execution"
  default_prompt: "Use $mandate402-backend for this task and follow its SKILL.md, references, and TESTS.md before implementing."
policy:
  allow_implicit_invocation: true

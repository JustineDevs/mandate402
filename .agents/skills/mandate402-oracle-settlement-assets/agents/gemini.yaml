interface:
  display_name: "Mandate402 Oracle Settlement Assets"
  short_description: "changing Mandate402 treasury settlement assets, token decimals, Pyth feeds, fiat conversion, stale oracle handling, facilitator settlement assumptions, or onchain payment amounts"
  default_prompt: "Use $mandate402-oracle-settlement-assets for this task and follow its SKILL.md, references, and TESTS.md before implementing."
policy:
  allow_implicit_invocation: true

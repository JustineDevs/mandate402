interface:
  display_name: "Typescript Best Practices"
  short_description: "reading or writing TypeScript or JavaScript files, TSX components, JS modules, or tsconfig settings"
  default_prompt: "Use $typescript-best-practices for this task and follow its SKILL.md, references, and TESTS.md before implementing."
policy:
  allow_implicit_invocation: true

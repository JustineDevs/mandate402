# mandate402-workers tests

## Should trigger

1. "Extract vendor dispatch into a worker-owned queue with leases."
2. "Move reconciliation from the operator route into a worker-owned runtime path."
3. "Add internal worker routes for deployed schedulers."

## Should not trigger alone

1. "Change the landing page CTA"
2. "Update the chain manifest"
3. "Add form-level validation to create mandate"

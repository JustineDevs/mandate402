# mandate402-postgres-persistence tests

## Should trigger

1. "Add a worker task column to the Postgres migration."
2. "Make the Supabase pooler URL handling safer."
3. "Fix store integrity checks for duplicate payment identifiers."

## Should not trigger alone

1. "Change the hero layout."
2. "Update Morph ABI names."
3. "Rewrite PR copy."

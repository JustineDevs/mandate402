name: Act Smoke

on:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: echo "This workflow exists to keep act-compatible entrypoints simple."
